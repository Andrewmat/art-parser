def normalCircle circle(r: 20, cx: 30, cy: 30)

svg(width: 400, height: 200) {
  def biggerCircle normalCircle(r: radius => radius * 1.7)

  biggerCircle(fill: 'red')
  normalCircle(fill: 'blue')

  normalCircle(
    fill: 'green',
    cx: parentX => parentX * 2,
    cy: parentY => parentY * 2
  )
}
